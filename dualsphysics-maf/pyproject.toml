[project]
name = "dualsphysics-maf"
version = "0.1.0"
description = "LLM-assisted workflow for DualSPHysics non-Newtonian simulations"
requires-python = ">=3.12"
dependencies = [
    # Install core directly (NOT the `agent-framework` meta-package): the meta-package's
    # `agent-framework-core[all]` extra pulls newer `agent-framework-ag-ui` releases that
    # require `agent-framework-core>=1.13.0`, conflicting with rc3 → ResolutionImpossible.
    # The code only needs core + `agent_framework.openai` (bundled in core).
    "agent-framework-core==1.0.0rc3",
    "mcp[fastmcp]>=1.0,<2",
    "openai>=2.0,<3",
    "pandas>=2.0,<4",
    "numpy>=2.0,<3",
    "jinja2>=3.1,<4",
    "pydantic>=2.0,<3",
    "pyvista>=0.44,<1",
    "matplotlib>=3.9,<4",
    "python-dotenv>=1.0,<2",
    "opentelemetry-semantic-conventions-ai==0.4.13",
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.34,<1",
]

[project.optional-dependencies]
test = [
    "pytest>=8.4,<9",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["main", "main_smoke", "gui_api"]

[tool.setuptools.packages.find]
include = ["agents*", "mcp_server*"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
python_files = ["test_*.py"]
markers = [
    "unit: fast, isolated tests for pure helpers and local file transformations",
    "integration: tests that require MCP, external binaries, or live services",
]
