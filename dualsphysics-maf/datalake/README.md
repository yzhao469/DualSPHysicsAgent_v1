# datalake

Working data directory for generated and runtime XML case definitions.

## Files in this folder

| File | Main function / logic |
|---|---|
| `.gitkeep` | Placeholder file to keep the `datalake` directory tracked when generated artifacts are cleaned. |
| `Case_Def.xml` | Active case-definition XML used as the mutable working input for build/simulation toolchain steps. |
